{
    "branch": "master",
    "public": "./",
    "commands": [
      "nvm i 16 && nvm use 16 && npm install gitbook-cli -g && gitbook install ./ && gitbook build",
      "mv _book/* $BUILD_DEST"
    ]
}